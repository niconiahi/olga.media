import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <section class="flex h-full flex-col items-end justify-center space-y-2">
      <h1 class="text-2xl text-brand-blue">Ranking bajo construcción</h1>
    </section>
  );
});
